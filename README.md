# p2.2-CPM-URV
MPI parallel sparse matrix multiplication (mD8K): processes compute partial products for the result matrix and communicate to assemble/validate the final output. Includes a performance study (runtime/speedup) in the PDF report and the MPI C implementation.
